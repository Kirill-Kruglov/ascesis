# Experiment 16: Consequence vs Feature

Synthetic causal-DAG experiment comparing shallow feature equivalence against verifier-derived consequence equivalence.

Run:

```bash
python scripts/run_consequence_vs_feature.py --seed 42 --max-depth 6 --num-dags 200
```
