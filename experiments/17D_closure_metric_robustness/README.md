# Experiment 17D: Closure Metric Robustness

This experiment tests whether Experiment 17C's closure-active subset is stable under independently defined internal metrics. It reuses the DAG generator, verifier, perturbation taxonomy, and base run parameters. No external labels, embeddings, LLM judgments, internet data, or hand-picked semantic categories are used.
