[project]
name = "latent-metric-geometry"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
  "pandas",
  "numpy",
  "scikit-learn",
  "scipy",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

