# Implementation Notes

The default run consumes 17D outputs and does not recompute perturbation attacks. Per-class Class-B labels are left as missing unless an attack-label table is passed with --attack-labels. All PCA/FA/ICA fits use internally derived metrics and structural descriptors only.
