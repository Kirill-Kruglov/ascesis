[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "backbone-stress"
version = "0.1.0"
description = "Experiment 17A: adversarial stress test for backbone consequence stability."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "pandas",
  "matplotlib",
  "pytest",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
