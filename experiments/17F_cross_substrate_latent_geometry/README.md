# Experiment 17F - Cross-Substrate Latent Geometry

Run:

```bash
python3 scripts/run_cross_substrate_latent_geometry.py
```

The default run uses seed 42 and 500 generated objects per non-causal substrate. It reuses 17E outputs for the causal-DAG baseline when available.

